## Escalera ascendente

### Code: 

```
function printAscendingStaircase(rows) {
  for (let i = 1; i <= rows; i++) {
    let line = '';
    for (let j = 1; j <= i; j++) {
      line += '*';
    }
    console.log(line);
  }
}

// Call the function and provide the number of rows for the ascending staircase
printAscendingStaircase(5);
```

### Result:

```
*
**
***
****
*****
```